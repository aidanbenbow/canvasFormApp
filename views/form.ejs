<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= form.title %></title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      padding: 20px;
    }
    .form-container {
      max-width: 600px;
      margin: 0 auto;
    }
  </style>
</head>
<body class="bg-light">
  <div class="container form-container">
    
    <h2 class="text-center"><%= form.title %></h2>

    <form action="/submitted" method="post">
      <input type="hidden" name="formId" value="<%= form.id %>">

      <div class="form-group">
        <label for="name"><%= form.name %></label>
        <input type="text" class="form-control" id="name" name="name" required>
      </div>

      <div class="form-group">
        <label for="ocupatie"><%= form.ocupatie %></label>
        <input type="text" class="form-control" id="ocupatie" name="ocupatie" required>
      </div>

      <div class="form-group">
        <label for="good"><%= form.good %></label>
        <textarea class="form-control" id="good" name="good" rows="3" oninput="autoResize(this)"></textarea>
      </div>

      <div class="form-group">
        <label for="better"><%= form.better %></label>
        <textarea class="form-control" id="better" name="better" rows="3" oninput="autoResize(this)"></textarea>
      </div>

      <div class="form-group">
        <label for="learnt"><%= form.learnt %></label>
        <textarea class="form-control" id="learnt" name="learnt" rows="3" oninput="autoResize(this)"></textarea>
      </div>

      <button type="submit" class="btn btn-primary btn-block">Submit</button>
    </form>
  </div>

  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script>
    function autoResize(textarea) {
      textarea.style.height = 'auto';
      textarea.style.height = (textarea.scrollHeight) + 'px';
    }
  </script>
</body>
</html>
