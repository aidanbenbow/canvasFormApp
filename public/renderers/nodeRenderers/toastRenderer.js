export const toastRenderStrategy = {
    render(node, ctx) {
      if (!node.visible) return;
  
      // Optional: draw nothing. Children draw themselves.
      // Optional: draw debug bounds or a transparent background.
    }
  };
  